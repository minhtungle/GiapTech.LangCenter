# 21/08/2026 — Rate limit (nợ N3) + FR-19 đăng ký đá trận qua link/QR

## N3: làm ở tầng API, không ở Caddy như kế hoạch ghi

Kế hoạch từ 17/08 ghi "rate limit ở tầng Caddy". Khi bắt tay làm mới thấy hai lý do phải đổi:

1. `caddy:2-alpine` **không có** module rate limit — nó là plugin bên thứ ba, dùng phải tự build
   bằng `xcaddy`. Thêm một image tự build vào chuỗi triển khai là thêm thứ phải bảo trì và phải
   rebuild mỗi lần Caddy ra bản vá.
2. Caddy **không đọc được body**, mà token lời mời nằm trong body (cố ý — URL vào log, history,
   Referer). Nên Caddy không phân biệt "một người dò 500 token" với "500 người mở link của mình".

.NET 8 có `Microsoft.AspNetCore.RateLimiting` sẵn trong framework. Cửa sổ **trượt** chứ không cố
định: cố định cho phép gấp đôi hạn mức ở chỗ giáp ranh (30 request cuối phút 1 + 30 đầu phút 2).

### Một giả định sai của tôi, sửa bằng cách ĐO

Comment ban đầu tôi viết "Caddy nối thêm vào cuối `X-Forwarded-For`" và code lấy phần tử **đầu**.
Không có căn cứ nào ngoài trí nhớ.

Dựng một Caddy thăm dò trước server echo để đo: client gửi `X-Forwarded-For: 9.9.9.9` thì server
nhận đúng `192.168.65.1`. **Caddy 2.11 GHI ĐÈ header đó, không nối thêm.** May là không thành lỗ
hổng (client không nhảy được khoá), nhưng code dựa trên giả định sai. Đổi sang phần tử **cuối** —
đúng cả ở cấu hình hiện tại lẫn khi chèn thêm proxy phía trước.

### Bảy phản chứng, hai lọt

| Phá gì | Lần đầu |
|---|---|
| Bỏ `[EnableRateLimiting]` khỏi một endpoint | ✅ đỏ |
| Bỏ `app.UseRateLimiter()` | ✅ đỏ |
| Bỏ `Retry-After` | ✅ đỏ |
| **Nới hạn mức endpoint GHI từ 10 lên 3000** | ❌ **xanh 5/5** |
| **Đổi mặc định cờ `GIOI_HAN_TAN_SUAT` thành false** | ❌ **xanh 5/5** |

Cái thứ tư lọt vì test chỉ so **tên policy**, không so **con số** — tên không đổi nên không bắt
được. Đưa hạn mức thành hằng số `public` để test đọc và khẳng định `GHI < ĐỌC`.

Cái thứ năm nghiêm trọng hơn: bộ test dùng factory **tự bật cờ**, nên nó không bao giờ thấy giá
trị mặc định. Đổi mặc định thành tắt là rate limit biến mất im lặng trên production. Thêm factory
**không đặt cờ** để canh đúng mặc định.

### Cờ tắt-trong-test là con dao hai lưỡi

Bật rate limit làm **112 integration test đỏ**: `TestServer` không mở socket thật nên
`RemoteIpAddress` là null với mọi request — tất cả test rơi vào chung một phân vùng và đốt hết hạn
mức của nhau. Nhận `QUA_NHIEU_YEU_CAU` thay vì dữ liệu.

Rồi lặp lại ở E2E: **8 test đỏ** vì 48 test cùng đăng nhập từ một IP, hạn `XacThuc` 10/phút.

Cách sai là nới hạn mức — production mất lớp bảo vệ chỉ vì test cần chạy nhanh. Cách đúng: tắt
bằng **cấu hình** ở đúng môi trường cần tắt (`ApiFactory` và `docker-compose.dev.yml`), và giữ một
factory tự bật cờ để cơ chế vẫn được canh.

## FR-19: ba yêu cầu, hai trong số đó là sửa thứ đang sai

Chủ sản phẩm muốn link/QR 5 phút → trang vote nhanh → chọn tên → khoá người đã chọn.

### Chữ "vote" là hai tính năng khác nhau

Mô tả *chọn tên → khoá* là **đăng ký đá trận**, không phải **vote MVP**. Với MVP thì "chọn tên
mình" phá vỡ tính bí mật của bầu chọn: một người biết link vote thay được cả đội, và quy tắc #8
(`UNIQUE` ở tầng DB) không cứu được vì nó **không biết ai thực sự bấm**. Lỗ hổng *thiết kế*, không
sửa được bằng thêm ràng buộc. → Chỉ làm cho đăng ký.

### "Khoá người đã chọn" nhằm sai mục tiêu

Nó chống *bấm hai lần*, nhưng cái cần chống là *mạo danh*. Người mở link đầu tiên thấy cả 18 tên,
chọn hộ người khác → người đó bị khoá, không đăng ký được, và không ai biết ai đã làm. **Cơ chế
khoá tự nó thành công cụ phá hoại.**

→ Cho sửa lại, đổi lấy dấu vết: đếm `SoLanSua` + cờ `QuaLink`. "Người này sửa 6 lần" là thứ trưởng
nhóm nhìn thấy được, còn "bị khoá oan" thì không.

### 5 phút quá ngắn cho luồng thật

Dán link vào nhóm chat → người đọc lúc đang lái xe → mở sau 20 phút → hết hạn → xin lại link. Một
việc thành ba. Nhưng cho chọn *tự do* thì sẽ có người đặt 30 ngày.

→ Bốn mốc cố định, chặn ở **validator** (dropdown là gợi ý; ai gọi API trực tiếp vẫn gửi được
525600 phút). Mốc 5 phút vẫn giữ — hợp lý khi chìa QR cho từng người quét tại chỗ.

### Hai thứ đang sai mà yêu cầu làm lộ ra

- **Chỗ đặt.** Lời mời đăng ký thuộc về *một trận*, nhưng nút tạo lại ở Hòm thư nên trưởng nhóm
  phải rời trận đang xem để đi tìm. Cùng loại lỗi với "thêm đối thủ phải rời form thêm trận"
  (17/08). → Tab thứ 5 trong chi tiết trận.
- **Không chọn được ai.** Handler luôn `db.CauThus.Select(c => c.Id)` — mời tất cả. Thực tế có
  người nghỉ dài, người chấn thương. → `CauThuIds`, null = mời tất cả (giữ hành vi cũ).

## Ba lỗi giao diện, không lỗi nào do test bắt

Cả ba lộ ra khi **chụp màn hình và nhìn**, trong khi 369 test backend xanh:

1. **`traLoi` là CHUỖI, frontend so với SỐ.** API trả `"ThamGia"` không phải `1`. Bảng hiện "Chưa
   trả lời" cho người đã trả lời, thống kê đếm 0, **không lỗi console**. Che mất lỗi: lệnh *ghi*
   nhận cả số lẫn chuỗi nên trả lời vẫn thành công — chỉ phần *đọc* sai.
2. **Danh sách tick về 16/16** dù lời mời chỉ 14 người (`useEffect` chạy trước khi `phanHois` tải
   xong). Bấm "Lưu danh sách" ngay sau khi mở tab sẽ **âm thầm mời lại** người vừa bỏ.
3. **Hiện `Tenant.SanNha` làm địa điểm trận.** Trận thoả thuận đá *Sân Tuyên Sơn* nhưng trang hiện
   *Sân Chi Lăng* — người đọc đến sai sân. Nguyên nhân: `TRAN_DAU` không có cột địa điểm, tôi lấy
   sân nhà CLB để lấp chỗ trống. Sửa: **không hiện gì** — sai còn tệ hơn thiếu.

## Một lần tôi đọc sai ảnh

Thấy "Đối đầu: chưa đá lần nào" và tưởng lỗi vì nhớ là hai CLB đã đá nhiều lần. Truy vấn DB: Hải
Châu FC đá với đối thủ tên "FC Hoà Xuân" đúng **1 trận**, và trận đó chính là trận đang xem
(`ket_qua = 0`). Câu đó **đúng**. Các trận tôi nhớ là với đối thủ khác.

## Chín phản chứng cho FR-19, một lọt

Lọt: nhồi `GhiChu` vào chính trường `hoTen` (`HoTen + " " + GhiChu`) → 16/16 vẫn xanh. Test khoá
**tên trường** nhưng không kiểm **giá trị**, và dữ liệu nhạy cảm chỉ đặt cho 1/3 cầu thủ nên hai
người kia sinh chuỗi rỗng. Siết: đặt cho cả 3 và khoá giá trị bằng regex chính xác.

## Kết quả

- Backend 353 → **370 test** (`GioiHanTanSuatTests` 6, `DangKyNhanhTests` 17).
- E2E 44 → **48 test** (`e2e/dang-ky-nhanh.spec.ts`).
- Migration chỉ `AddColumn` + 1 filtered UNIQUE. Đã `pg_dump` trước; sau khi áp: 7 CLB / 39 cầu
  thủ / 64 trận / 34 phản hồi / 330 vote **giữ nguyên**, 34 hàng cũ nhận `so_lan_sua=0`.
- Nợ N3 đóng. FR-19 xong.

---

## Đổi lưới checkbox thành select — và lỗi đua quay lại lần hai

Chủ sản phẩm yêu cầu phần "Ai được mời" dùng select giống chỗ chọn thành viên ở tab Đội hình,
*"rất dễ thao tác"*. Lý do rõ khi dùng thật: đội 20+ người thì lưới 3 cột dài hơn cả màn hình,
phải cuộn để tìm một người, và **không gõ tên để tìm được**.

Dùng lại `SelectTimKiemNhieu` có sẵn, gói trong khối gập được cùng khuôn tab Đội hình để hai chỗ
chọn người trông như một. Giữ "Chọn tất cả / Bỏ hết" vì mời cả đội là ca thường gặp nhất.

### Lỗi đua ở CÙNG một chỗ, lần thứ hai trong ngày

Chụp màn hình sau khi đổi: nhãn hiện **16/16** trong khi lời mời có **14** — đúng lỗi tôi đã sửa
vài giờ trước, quay lại.

Sửa lần 1 chỉ chờ `phanHois`. Nhưng `dangChoPhanHoi = !!loiMoi?.id && phanHois === undefined` chỉ
đúng khi `loiMoi` **đã** tải; trong khoảnh khắc `loiMoi === undefined` thì cả hai điều kiện đều
false nên effect chạy ngay và tick hết 16, rồi `daChon !== null` chặn mọi lần sửa sau.

Điều kiện đúng là "chưa biết đủ thì chưa quyết định": `dangTaiLoiMoi || (loiMoi?.id && !phanHois)`.

### Phản chứng lọt hai lần, và tôi dừng lại ĐO thay vì đoán tiếp

Bản lỗi lần 1 vẫn **4/4 xanh** cả với `reload()` lẫn với context mới không cache. Thay vì thêm
phỏng đoán, tôi viết một script đo thứ tự request và trạng thái nhãn theo từng 100ms:

```
89935 200 /cau-thu?soDong=200
89935 200 /hom-thu/dang-ky/theo-tran/...      ← CÙNG millisecond
89949 200 /hom-thu/dang-ky/.../phan-hoi
NHÃN: (0/0)  →  (14/16)                       ← không bao giờ qua (16/16)
```

Kết luận: trên localhost hai query về cùng lúc nên nhánh sai **không kịp chạy**. Lỗi chỉ lộ khi
`loiMoi` về sau `cauThus` — cần mạng chậm hoặc `route()` làm chậm có chủ đích.

Tôi **không** viết test giả vờ canh được điều đó. Đã ghi rõ giới hạn ngay trong test: phần được
canh là *kết quả cuối* (2/3, không âm thầm mời lại người đã bỏ), phần **không** canh được là
khoảnh khắc đua. Thêm độ trễ giả vào E2E dễ thành nguồn flake mới, mà nợ N8 đang là flake ~1/48.

Phản chứng "bỏ hết điều kiện chờ" thì **đỏ** — nên test vẫn có giá trị, chỉ không phủ hết.

### Bốn lần selector sai trong test

Đều vì tôi đoán cấu trúc DOM thay vì đọc:

1. `page.locator('tbody tr').click()` — dòng bảng không có `onClick`, phải bấm nút trong dòng.
2. `button[title="Mở chi tiết"]` — title thật là "Đội hình · Sơ đồ · Đánh giá".
3. `text=/Ai được mời \(2\/3\)/` — tên và số ở hai `<span>` khác nhau nên regex một dòng không khớp.
4. `button[aria-label^="Bỏ "]` — nút × là `<span role="button">`, không phải `<button>`.

Bài học lặp lại: với selector, đọc component rồi viết, đừng viết rồi chạy thử.

### Kết quả

370 test backend + **48 E2E** xanh. Không đổi API, không migration.

---

## Sidebar giãn theo chiều cao trang

Chủ sản phẩm báo: *"khi màn hình dài thì sidebar cũng bị kéo dài theo"*.

Đo trước khi sửa, trên màn Thống kê với viewport 700px:

```
trang cao    : 1413
sidebar cao  : 1412        ← giãn theo cả trang
sau khi cuộn xuống đáy: sidebar top = -713
```

`top = -713` là hậu quả thật: logo và toàn bộ menu trôi khỏi màn hình, người dùng ở đáy trang
phải cuộn ngược lên mới đổi được trang.

Nguyên nhân: `<aside>` là flex item thường trong container `min-h-screen`, nên `align-items:
stretch` kéo nó cao bằng container — mà container cao theo nội dung.

Sửa một chỗ: `sticky top-0 h-screen overflow-hidden`. `h-screen` khoá đúng một viewport,
`sticky` giữ nó tại chỗ khi trang cuộn.

**`nav` đã có `overflow-y-auto` từ trước nhưng vô hiệu** — thuộc tính đó chỉ có tác dụng khi phần
tử cha bị giới hạn chiều cao. Sau khi khoá `h-screen` thì nó hoạt động: kiểm ở viewport 420px,
menu cuộn được trong sidebar (nội dung 548px trong khung 287px) và nút Đăng xuất vẫn nằm trong
tầm nhìn thay vì bị đẩy ra ngoài.

Ngăn kéo mobile dùng `fixed` nên độc lập — kiểm lại: `top=0`, cao 700 = viewport, không ảnh hưởng.

### Ba phản chứng, cả ba đỏ

| Phá gì | Kết quả |
|---|---|
| Bỏ `h-screen` + `sticky` (bản lỗi gốc) | ✅ đỏ **cả 2** test |
| Có `h-screen` nhưng bỏ `sticky` | ✅ đỏ 1 |
| Bỏ `overflow-y-auto` ở `nav` | ✅ đỏ 1 |

### Hai lỗi trong test của tôi

1. **Chốt an toàn tự bắt tôi.** Test dùng `/thong-ke`, nhưng CLB test mới không có dữ liệu nên
   trang ngắn hơn viewport — test sẽ xanh mà không kiểm được gì. Câu `expect(trang).toBeGreaterThan(vh + 100)`
   tôi đặt phòng đúng ca đó đã đỏ. Sửa: tạo 12 cầu thủ để bảng dài, không phụ thuộc trận/kết quả.
2. **`toBe(0)` vs `-0`.** `Math.round(-0.2)` cho `-0`, mà `toBe` phân biệt `-0` với `0` nên test
   đỏ dù sidebar đúng vị trí. Đổi sang `toBeCloseTo(0, 0)`.

Loại lỗi này **không test nào bắt được trước đó** vì nó không làm gì đỏ — mọi phần tử vẫn đúng chỗ
trong DOM, chỉ hình học sai. Nay có `e2e/sidebar.spec.ts` canh.

E2E 48 → **50 test**.

---

## Gọn lại nội dung: đo trước, sửa chỗ thật sự cần

Chủ sản phẩm yêu cầu bảng người vote trong tab Đăng ký cho cuộn, *"ưu tiên gọn gàng"*, và **mọi
màn khác nếu chưa gọn thì thiết kế lại**.

Yêu cầu thứ hai dễ thành sửa bừa 13 màn. Nên **đo trước** — viewport 1440×800:

| Trang | Cao | Vượt |
|---|---|---|
| Hòm thư | 1719 | +919 |
| Thiết lập | 1420 | +620 |
| Thống kê | 1413 | +613 |
| Lịch thi đấu | 1215 | +415 |
| Cầu thủ | 1019 | +219 |
| tab Đăng ký | 1060 | +260 |
| **8 màn còn lại** | **800** | **vừa khít** |

Không màn nào cuộn ngang. Vậy chỉ 6 chỗ cần sửa, 8 chỗ để nguyên — sửa chúng là tạo rủi ro không
đổi lấy gì.

### Bốn nguyên nhân khác nhau, bốn cách sửa khác nhau

**1. Bảng dài đẩy trang.** Thêm prop `caoToiDa` cho `Table` — giới hạn chiều cao + **header dính**.
Không đặt mặc định cho cả 16 bảng trong dự án: bảng 3–5 dòng mà có khung cuộn riêng trông như bị
hỏng. Chỉ áp cho 3 bảng đã đo là dài (Lịch 20 dòng, Thống kê 16, Cầu thủ 16) + bảng Đăng ký.

**2. Cột gần như rỗng.** Bảng Đăng ký có 5 cột, nhưng "Nguồn" và "Lời nhắn" phần lớn là dấu "—"
(chỉ ai trả lời qua link mới có badge, chỉ ai ghi chú mới có chữ). Hai cột chiếm 1/3 bề ngang để
hiển thị gần như không gì. Gộp vào cột trả lời → 3 cột, mỗi dòng thấp hơn, mắt chỉ quét một cột.
Thêm màu theo câu trả lời để phân biệt không cần đọc chữ.

**3. Mỗi trường một dòng riêng.** Thẻ lời mời thách đấu ở Hòm thư: 8 dòng cho nội dung vừa 3 dòng,
mỗi thẻ cao ~230px. Gộp giờ · sân · liên hệ vào MỘT hàng (chúng đều là "thông tin trận, đọc một
lượt") và giảm `gap-2.5` → `gap-1.5`. Hòm thư 1719 → **1471**.

**4. Bố cục sai, không phải nội dung nhiều.** Thiết lập dùng `max-w-2xl` (672px) trên màn 1440px —
bỏ trống hoàn toàn nửa phải, và hai khối ảnh **đã có `flex-wrap`** nhưng xếp dọc vì container quá
hẹp. Nới `max-w-5xl`: Logo + Ảnh bìa nằm cạnh nhau, 8 màu áo một hàng. 1420 → **1242**.

Không để rộng vô hạn: dòng nhập dài quá 3–4 inch làm mắt mất điểm neo khi nhảy sang dòng sau.

**Phụ:** `TrangTrong` dùng `py-12` — khối "Chưa có lời mời nào" chiếm 150px chỉ để nói một câu,
trong khi đây là trạng thái *rỗng*, không nên chiếm chỗ hơn nội dung thật. Giảm còn `py-6`.

### Kết quả đo lại

| Trang | Trước | Sau |
|---|---|---|
| tab Đăng ký | 1060 | **853** |
| Lịch thi đấu | 1215 | **800** |
| Cầu thủ | 1019 | **800** |
| Thống kê | 1413 | **1042** |
| Hòm thư | 1719 | **1471** |
| Thiết lập | 1420 | **1242** |

Ba trang vừa khít viewport. Thống kê còn 242 vì biểu đồ + 4 thẻ KPI + bảng là nội dung thật (đã
nén biểu đồ 280→220px). Thiết lập còn 442 vì form thật sự có 14 trường — khác hẳn 620px lãng phí
do bố cục sai.

### Hai test đỏ vì chính thay đổi này

Không phải hồi quy, mà là hệ quả:

1. **`sidebar.spec.ts`** — chốt an toàn `expect(trang > viewport + 100)` báo "không kiểm được gì",
   vì bảng Cầu thủ giờ cuộn nên trang chỉ 700px. Đây là **lần thứ ba** chốt đó tự bắt tôi. Đổi
   sang `/quan-tri/thiet-lap`: form 14 trường, luôn dài với mọi CLB kể cả CLB rỗng, và không có
   bảng nên không bị ảnh hưởng nếu sau này tôi cho bảng khác cuộn.
2. **`dang-ky-nhanh.spec.ts`** — bảng Lịch giờ cuộn trong khung, dòng mới nằm ngoài vùng nhìn nên
   `click` timeout. Thêm `scrollIntoViewIfNeeded`.

### Ba lần tôi mắc cùng một lỗi cú pháp

Đặt `{/* comment */}` ngay trước root element của `return` — JSX không cho, và tôi làm ba lần liền
(`index.tsx`, `LichThiDau.tsx`, `ThietLap.tsx`). Chuyển thành doc comment trên hàm.

Và một lỗi thấy trên ảnh: `Số áo` gãy hai dòng ở `w-14` làm header cao thêm → `w-16 whitespace-nowrap`.
