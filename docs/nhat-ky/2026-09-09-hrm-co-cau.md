# 2026-09-09 (HRM) — FR-22 Cơ cấu tổ chức

## Phân tích đề xuất trước khi code

Chủ sản phẩm đưa 4 ý. Việc đầu tiên là nhận ra chúng **không cùng một tầng** — và ý thứ ba trộn
hai khái niệm:

> "Vai trò mặc định (chức vụ): ban quản lý, nhân viên kinh doanh, giáo viên, trợ giảng"

Danh sách này lẫn **chức danh** với **loại nghiệp vụ**:

- `GiaoVien`/`TroGiang` đã là `LoaiNguoiDung`, và nó **load-bearing**: 6+ chỗ trong LMS/CRM lọc
  theo nó (ai gán được vào lớp, ai ghi danh được, hồ sơ con nào được tạo).
- "Ban quản lý" **không** phải loại nghiệp vụ — nhét vào enum sẽ tạo một giá trị mà không màn
  nghiệp vụ nào biết xử lý.
- "Nhân viên kinh doanh" đã đặc biệt sẵn, nhưng qua **quyền** `ChucNang.NhanVienKinhDoanh` —
  đúng chỗ của nó (quy tắc #9).

Chốt: `LoaiNguoiDung` giữ nguyên 4 giá trị; chức vụ thành bảng danh mục riêng (FR-24). Một người
có cả hai: `LoaiNguoiDung = GiaoVien` + `ChucVu = "Trưởng bộ môn"`.

## Bốn chỗ tôi nói sai trong bản đề xuất đầu, và cách phát hiện

Chủ sản phẩm yêu cầu "xem lại cho đúng". Việc rà lại tìm ra:

**1. Nói quá về giáo viên và phòng ban.** Tôi bảo backend làm giáo viên "không xếp được vào phòng
ban" — sai. `GhiHoSo` nhận `HoSoNhanVien` nếu client gửi DTO, bất kể vai trò. Chỉ **frontend**
không gửi khối đó cho giáo viên, và comment tại chỗ ghi rõ lý do (gửi cả ba khối sẽ ghi rỗng đè
lên hồ sơ vai trò khác — quy tắc #1).

Kết luận vẫn giữ nhưng **vì lý do mạnh hơn**, tìm được trong DB thật: **một trợ giảng đang còn
`HO_SO_NHAN_VIEN`** từ hồi làm nhân viên (đổi vai trò không xoá hồ sơ cũ). Phòng ban nằm ở hồ sơ
vai trò thì không trả lời được "phòng ban HIỆN TẠI của người này".

**2. Hàm chống chu trình có lỗi thật.** Bản đầu truy vấn từng cấp (N query theo độ sâu), và
`FirstOrDefaultAsync` trên `Guid?` trả `null` cho **hai trường hợp khác nhau** — phòng không tồn
tại, và phòng là gốc. Viết lại: tải một lần cặp (id, cha), đi ngược trong bộ nhớ.

**3. Sĩ số phòng đếm sai được.** Cách 2 chặn học viên, nhưng **cách 1** (form hồ sơ) cũng đặt
`PhongBanId` mà không chặn. Đã lọc học viên ở **cả tầng đọc**, không chỉ tầng ghi.

**4. FR thiếu 4 mã lỗi** mà handler thật đang throw.

## Hai phát hiện từ dữ liệu thật — đổi kế hoạch FR-24

Đếm trong DB thay vì đoán:

| Cột | Dữ liệu thật | Hệ quả |
|---|---|---|
| `HO_SO_NHAN_VIEN.phong_ban` | **NULL cả 44 hàng** | Bỏ tự do, không mất gì |
| `HO_SO_NHAN_VIEN.chuc_vu` | **39 hàng có dữ liệu** | FR-24 phải sinh danh mục từ giá trị đang có rồi nối lại |

Nếu không đếm, FR-24 sẽ là một migration xoá 39 hàng dữ liệu thật. Đã kiểm sau khi bỏ cột
`phong_ban`: `chuc_vu` còn đủ 39 hàng.

## PostgreSQL: NULL trong UNIQUE index — kiểm chứ không tin trí nhớ

`UNIQUE(tenant_id, phong_ban_cha_id, ten)` **không** chặn hai phòng gốc trùng tên vì `NULL ≠ NULL`.
Thử trực tiếp:

```sql
create unique index on t (a, b, ten);
insert into t values (1, null, 'X');
insert into t values (1, null, 'X');   -- vẫn vào được
-- → 2 dòng
```

Nên phải thêm partial index `WHERE phong_ban_cha_id IS NULL`. Có test riêng đọc `index.GetFilter()`
— `[Theory]` sẵn có không phân biệt được (cùng cột, khác filter).

## Vấp phải

### Quên migration → hỏng cả môi trường dev

Thêm `PhongBanId` vào entity mà chưa tạo migration, nên API chạy với schema cũ và **mọi endpoint
tạo người dùng trả 500** (`column "phong_ban_id" does not exist`). Chỉ lộ ra khi chạy E2E — 4 test
sidebar đỏ ở bước tạo trung tâm, kể cả 2 test cũ không liên quan.

Bài học: sửa entity xong thì tạo migration **ngay**, đừng để "làm sau" — build vẫn xanh, test
in-memory vẫn xanh, chỉ môi trường thật là hỏng.

### `Guid?` không dùng được quy ước null-vs-rỗng

`AnhDaiDienUrl` phân biệt được "không gửi" (`null`) với "gỡ ảnh" (`''`) vì nó là chuỗi. `Guid?`
chỉ có **một** giá trị trống, nên phải thêm cờ `DoiPhongBan` riêng. Không có cờ thì mọi form không
có ô phòng ban sẽ âm thầm gỡ người khỏi cơ cấu mỗi lần lưu — đúng lỗi 16/08 với ô địa chỉ.

Đã kiểm 4 ca trên API thật: không gửi → giữ; `doiPhongBan=true` + null → gỡ; + id → đổi; đổi vai
trò sang học viên → tự rời.

### `Guid?` cũng không dùng được làm khoá Dictionary

`ToDictionary` có ràng buộc `notnull`. Dùng `Guid.Empty` đại diện gốc cây — an toàn vì Id thật
không bao giờ là Empty.

### Optional argument trong expression tree

Thêm `Guid? PhongBanId = null` vào `NguoiDungDto` làm EF đỏ: `CS0854 — An expression tree may not
contain a call that uses optional arguments`. Bỏ giá trị mặc định, truyền tường minh ở phép chiếu.

### `TreatWarningsAsErrors` chặn cả đột biến mã

Muốn đột biến `if (request.DoiPhongBan)` thành `if (true)` để kiểm test — build đỏ vì
`CS0162 Unreachable code`. Phải viết `if (!x || x)` mới qua được. Ghi lại vì lần sau còn gặp.

## Kiểm chứng

Ngoài 17 test tự động, đã chạy tay trên PostgreSQL thật (tenant `3EWWR64`):

| Kiểm | Kết quả |
|---|---|
| Dựng cây BGĐ > (Đào tạo > Bộ môn Anh, Kinh doanh) | Cây lồng đúng, sĩ số cả nhánh cộng dồn ✓ |
| **Xếp GIÁO VIÊN vào Bộ môn Anh** (yêu cầu chính) | `loai_nguoi_dung=1` + phòng ban ✓ |
| Gán BGĐ làm con của hậu duệ | `PHONG_BAN_CHU_TRINH` ✓ |
| Gán chính nó làm cha | `PHONG_BAN_CHU_TRINH` ✓ |
| Xoá phòng còn con / còn người | `PHONG_BAN_CON_CAP_DUOI` / `PHONG_BAN_CON_NGUOI` ✓ |
| Xếp học viên (cả 2 đường vào) | `HOC_VIEN_KHONG_VAO_CO_CAU` ✓ |
| Trùng tên cùng cha / khác cha | chặn / cho phép ✓ |
| Gán học viên làm quản lý | `NGUOI_QUAN_LY_KHONG_HOP_LE` ✓ |
| Lái UI: form giáo viên | Có ô Phòng ban, nhãn mang đường dẫn cha ✓, 0 lỗi console |
| Migration bỏ cột chuỗi | 44 hồ sơ · 39 `chuc_vu` · 64 người — **không đổi** ✓ |

## Bổ sung: làm màn cây (và một lỗi tự gây khó chịu)

Chủ sản phẩm: *"sao tôi chưa thấy module mới"*. Đúng — tôi làm API, handler, migration, 17 test,
tài liệu... **mà không có route và không có mục menu**. Nghĩa là với người dùng thì FR-22 chưa tồn
tại. Bài học đơn giản mà đắt: một module chỉ "xong" khi bấm được từ sidebar.

### Ba lần sai với thư viện, và cách tìm ra

Cài `@headless-tree` rồi cây **rỗng hoàn toàn**: menu đúng, route đúng, API trả 5 phòng, 0 lỗi
console, mà `getItems()` trả `[]`.

Ba lần thử đều dựa vào suy đoán từ file `.d.ts` và đều sai:

1. `initialState: { expandedItems: ... }` — vô dụng, vì nó chỉ đọc một lần lúc mount, khi đó
   query chưa xong.
2. `rebuildTree()` trong effect — vẫn rỗng.
3. `scheduleRebuildTree()` — vẫn rỗng.

Chỉ khi **viết một script Node độc lập** dựng cây bằng dữ liệu giả mới thấy sự thật:

```
1) getItems ngay sau createTree: 0
2) sau setMounted(true) + rebuildTree: 2
4) sau setState({expandedItems:['a']}): KHÔNG thêm con
   sau item.expand():                  CÓ thêm con
```

Hai điều không đọc ra được từ type: `getItems()` rỗng cho tới lần rebuild đầu tiên, và mở nhánh
**phải** qua `item.expand()` — `setState({ expandedItems })` đổi state nhưng `getItems()` không
trả con. Nên thứ tự đúng là `rebuildTree()` **rồi mới** `expand()`.

Bài học: khi thư viện mới không chạy như mong đợi, dừng đoán trong app — dựng một script 20 dòng
với dữ liệu giả. Ba lần thử trong app tốn nhiều thời gian hơn một lần đo.

### `level` bắt đầu từ 0

Tôi viết `paddingLeft: (level - 1) * 1.5rem` theo thói quen → hàng trên cùng có padding **âm**.
Đo lại: hàng trên cùng là `L0`. Sửa thành `0.625 + level * 1.5`.

### Mở sẵn cả cây, không chỉ cấp 1

Ban đầu chỉ mở `level === 0`. Cơ cấu tổ chức là thứ người ta vào để NHÌN, nên mở hết. Phải **lặp**
vì mở một cấp thì cấp dưới mới xuất hiện trong `getItems()` — có chặn số lượt theo số phòng để
dữ liệu có chu trình không làm treo.

### Hai lần selector test sai, không phải app sai

Dropdown nhân sự "rỗng" → hoá ra tôi tìm `div.absolute li`, mà `SelectTimKiem` dùng
`role="option"`. Lần khác nút "Thêm" trong modal không bấm được vì selector quá rộng. Cả hai lần
API và UI đều đúng. Nhắc lại bài học 09/09 buổi sáng: khi kết quả đo trái với ảnh chụp, **nghi
phép đo trước**.

### Kiểm chứng qua UI thật

| Kiểm | Kết quả |
|---|---|
| Mục menu + route | ✅ "Cơ cấu tổ chức" đầu nhóm HRM |
| Cây 5 phòng, 3 cấp | ✅ thụt lề đúng, sĩ số riêng/cả nhánh |
| Thu nhánh gốc | ✅ 6 → 1 node |
| Thêm phòng cấp dưới | ✅ "Phòng Kế toán" hiện ngay |
| Menu thao tác | ✅ 4 mục đúng quyền |
| Cách 2: danh sách nhân sự | ✅ 4 người, hiện phòng hiện tại, **không có học viên** |
| Xếp người vào phòng | ✅ sĩ số nhánh 1 → 2 |
| Bundle | 731 → 767 KB (+~10 KB gzip) |

## Còn nợ

- **Kéo-thả đổi cha** chưa làm (thư viện có `dragAndDropFeature`, chưa bật). Nay đổi cha bằng
  cách sửa phòng ban.
- Cảnh báo thư viện đã ghi lại để khỏi tra lại: `react-organizational-chart` là kết quả npm top
  khi tìm "org chart" nhưng release cuối 04/2023 và kéo `@emotion/css` trùng vai Tailwind;
  `react-arborist` ghim `react-dnd@14` có issue React 19 còn mở; đừng dùng thư viện sơ đồ
  (`@xyflow/react`, 58 KB gzip + engine layout) cho một treeview 2-4 cấp.
- FR-23 (hồ sơ mở rộng), FR-24 (danh mục chức vụ).
- **Phạm vi theo phòng ban** (trưởng phòng chỉ thấy người phòng mình) — cần tầng
  `IPhamViPhongBan` riêng, tuyệt đối không suy ngầm từ `nguoi_quan_ly_id`.
- Lịch sử điều chuyển phòng ban.
