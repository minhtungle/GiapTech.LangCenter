namespace GiapTech.LangCenter.Application;

/// <summary>
/// Điểm neo để tham chiếu assembly Application — dùng khi đăng ký MediatR/FluentValidation
/// (quét handler, validator) và cho test luật phụ thuộc.
/// </summary>
public static class AssemblyReference
{
}
