namespace GiapTech.LangCenter.Domain;

/// <summary>
/// Điểm neo để tham chiếu assembly Domain (dùng cho test luật phụ thuộc và đăng ký DI)
/// mà không phải phụ thuộc vào một entity cụ thể nào.
/// </summary>
public static class AssemblyReference
{
}
